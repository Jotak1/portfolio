---
import BaseLayout from '../layouts/BaseLayout.astro';
import { profile } from '../content/profile';
import '../styles/pages/expediente.css';

const base = import.meta.env.BASE_URL;
const {
  name,
  shortName,
  role,
  location,
  education,
  email,
  links,
  hero,
  bioHeadline,
  bio,
  ai,
  stack,
  contact,
  experience,
} = profile;
const familyName = name.slice(shortName.length).trim();
---

<BaseLayout title={`Expediente · ${name}`} description={profile.seoDescription} bodyClass="">
  <a class="skip-link" href="#content">Saltar al contenido</a>
  <div class="progress" id="scroll-progress" role="progressbar" aria-hidden="true"></div>

  <header class="topnav" data-od-id="topnav">
    <div class="container topnav-inner">
      <a class="logo" href={base} data-transition-label="Inicio" data-od-id="logo">
        <span class="logo-given">{shortName}</span>
        <span class="logo-family">{familyName}</span>
      </a>
      <nav aria-label="Secciones del expediente">
        <a href="#ia" data-od-id="nav-ia">IA</a>
        <a href="#trayectoria" data-od-id="nav-trayectoria">Trayectoria</a>
        <a href="#stack" data-od-id="nav-stack">Stack</a>
        <a href="#contacto" data-od-id="nav-contacto">Contacto</a>
      </nav>
      <div class="topnav-actions" data-od-id="topnav-actions">
        <a class="btn btn-secondary" href={`${base}resonancia/`} data-transition-label="Resonancia" data-od-id="nav-resonancia">Vista Resonancia</a>
      </div>
    </div>
  </header>

  <main id="content" tabindex="-1">
    <section class="section hero" data-od-id="hero" aria-labelledby="hero-title">
      <div class="container hero-split">
        <div>
          <p class="eyebrow">{hero.eyebrow}</p>
          <h1 data-od-id="hero-title" id="hero-title">
            <span id="title-l1">{hero.titleLine1}</span>
            <span id="title-l2" style="display:block">{hero.titleLine2}</span>
          </h1>
          <p class="lead" style="margin-top: 20px;">{hero.lead}</p>
          <div class="hero-cta">
            <a class="btn btn-primary" href={`${base}resonancia/`} data-transition-label="Resonancia" data-od-id="cta-resonancia">Entrar a Resonancia</a>
            <a class="btn btn-ghost btn-arrow" href="#trayectoria" data-od-id="cta-leer">Ver trayectoria</a>
          </div>
        </div>
        <aside class="signal-card" data-od-id="signal-preview" aria-label={`Señal activa: ${hero.signalRole}, ${hero.signalJob}`}>
          <div>
            <p class="meta">{hero.signalLabel}</p>
            <p style="font-family: var(--font-display); font-size: 28px; margin-top: 10px; letter-spacing: -0.02em;">{hero.signalRole}</p>
          </div>
          <div class="signal-wave" data-od-id="signal-wave" aria-hidden="true">
            <svg viewBox="0 0 280 120" preserveAspectRatio="xMidYMid meet" focusable="false">
              <defs>
                <linearGradient id="signal-wave-grad" x1="0" y1="0" x2="0" y2="1">
                  <stop offset="0%" stop-color="var(--accent)" stop-opacity="0.35" />
                  <stop offset="100%" stop-color="var(--accent)" stop-opacity="0" />
                </linearGradient>
              </defs>
              <path class="wave-ghost" id="hero-wave-ghost" d="" />
              <path class="wave-fill" id="hero-wave-fill" d="" />
              <path class="wave-harm" id="hero-wave-harm" d="" />
              <path class="wave-path" id="hero-wave" d="" />
            </svg>
          </div>
          <p class="meta">{hero.signalJob}</p>
        </aside>
      </div>
    </section>

    <section class="section" data-od-id="bio" aria-labelledby="bio-title">
      <div class="container grid-2-1">
        <div class="stack reveal" style="gap: 20px; max-width: 54ch;">
          <p class="eyebrow" style="color: var(--muted);">Quién soy</p>
          <h2 id="bio-title">{bioHeadline}</h2>
          <p class="lead">{bio[0]}</p>
          <p style="color: var(--muted);">{bio[1]}</p>
        </div>
        <aside class="card reveal" data-od-id="bio-side" aria-label="Perfil resumido">
          <p class="meta" style="margin-bottom: 8px;">Perfil</p>
          <div class="stack" style="gap: 0;">
            <div class="profile-row">
              <p class="meta">Rol</p>
              <p>{role}</p>
            </div>
            <div class="profile-row">
              <p class="meta">Foco</p>
              <p>IA aplicada</p>
            </div>
            <div class="profile-row">
              <p class="meta">Ubicación</p>
              <p>{location}</p>
            </div>
            <div class="profile-row">
              <p class="meta">Educación</p>
              <p>{education}</p>
            </div>
          </div>
        </aside>
      </div>
    </section>

    <section class="section" data-od-id="ia" id="ia" aria-labelledby="ia-title">
      <div class="container grid-2-1">
        <div class="stack reveal" style="gap: 20px; max-width: 54ch;">
          <p class="eyebrow" style="color: var(--muted);">{ai.eyebrow}</p>
          <h2 id="ia-title">{ai.headline}</h2>
          <p class="lead">{ai.intro}</p>
        </div>
        <aside class="card reveal" data-od-id="ia-systems" aria-label="Sistemas de IA aplicada">
          <p class="meta" style="margin-bottom: 8px;">Sistemas</p>
          <div class="stack" style="gap: 0;">
            {ai.systems.map((system) => (
              <div class="profile-row">
                <p class="meta">{system.title}</p>
                <p>{system.summary}</p>
              </div>
            ))}
          </div>
        </aside>
      </div>
    </section>

    <section class="timeline-section" data-od-id="trayectoria" id="trayectoria" aria-label="Trayectoria profesional">
      <div class="timeline-pin" id="timeline-pin">
        <div class="timeline-hud">
          <p class="eyebrow" style="color: var(--muted); margin-bottom: 4px;">Trayectoria · 2019 – hoy</p>
          <div class="timeline-year num" id="timeline-year" aria-live="polite">{experience[0]?.year ?? '2025'}</div>
          <div class="timeline-meta">
            <p class="meta" id="timeline-label">{experience[0]?.company ?? ''}</p>
            <p class="meta" aria-hidden="true"><span id="timeline-idx">01</span> / {String(experience.length).padStart(2, '0')}</p>
          </div>
        </div>
        <div class="timeline-track" id="timeline-track">
          {experience.map((job) => (
            <article
              class="timeline-card"
              data-year={job.year}
              data-label={job.company}
              data-od-id={`exp-${job.id}`}
            >
              <div>
                <p class="role">{job.roleLine}</p>
                <h3>{job.company}</h3>
                <p>{job.summary}</p>
              </div>
              <p class="meta">{job.badge}</p>
            </article>
          ))}
        </div>
        <div class="timeline-rail" aria-hidden="true"><i id="timeline-fill"></i></div>
      </div>
    </section>

    <section class="section" data-od-id="stack" id="stack" aria-labelledby="stack-title">
      <div class="container stack" style="gap: 40px;">
        <div class="reveal" style="max-width: 36ch;">
          <p class="eyebrow" style="color: var(--muted);">Stack</p>
          <h2 id="stack-title">{stack.headline}</h2>
        </div>
        <div class="grid-3">
          <div class="card reveal" data-od-id="stack-front">
            <p class="meta" style="margin-bottom: 12px;">Front</p>
            <div class="chips" style="margin-top:0">
              {stack.frontend.map((tag) => <span class="tag">{tag}</span>)}
            </div>
          </div>
          <div class="card reveal" data-od-id="stack-back">
            <p class="meta" style="margin-bottom: 12px;">Back</p>
            <div class="chips" style="margin-top:0">
              {stack.backend.map((tag) => <span class="tag">{tag}</span>)}
              {stack.dataOps.map((tag) => <span class="tag">{tag}</span>)}
            </div>
          </div>
          <div class="card reveal" data-od-id="stack-ia">
            <p class="meta" style="margin-bottom: 12px;">IA</p>
            <div class="chips" style="margin-top:0">
              {stack.ai.map((tag) => <span class="tag">{tag}</span>)}
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="section" data-od-id="contacto" id="contacto" style="text-align: center;" aria-labelledby="contacto-title">
      <div class="container reveal" style="max-width: 560px;">
        <p class="eyebrow" style="color: var(--muted);">Contacto</p>
        <h2 id="contacto-title">{contact.headline}</h2>
        <p class="lead" style="margin: 16px auto 28px;">{contact.pitch}</p>
        <div class="hero-cta" style="justify-content: center;">
          <a class="btn btn-primary" href={`mailto:${email}`} data-od-id="cta-mail">{email}</a>
        </div>
        <div class="chips" style="justify-content: center; margin-top: 24px;" role="list">
          <a class="tag" role="listitem" href={links.linkedin} target="_blank" rel="noopener noreferrer" data-od-id="link-linkedin" aria-label="LinkedIn (se abre en una pestaña nueva)">LinkedIn</a>
          <a class="tag" role="listitem" href={links.github} target="_blank" rel="noopener noreferrer" data-od-id="link-github" aria-label="GitHub (se abre en una pestaña nueva)">GitHub</a>
          <a class="tag" role="listitem" href={`${base}resonancia/`} data-transition-label="Resonancia" data-od-id="link-resonancia">Probar Resonancia</a>
        </div>
      </div>
    </section>
  </main>

  <footer class="pagefoot" data-od-id="footer">
    <div class="container row-between" style="flex-wrap: wrap;">
      <span>© 2026 {name}</span>
      <span class="meta">Expediente · {location}</span>
    </div>
  </footer>
</BaseLayout>

<script>
  import { bootMotion } from '../scripts/boot.ts';
  import { initExpedientePage } from '../scripts/expediente-page.js';
  bootMotion({ scrollTrigger: true }).then(() => initExpedientePage());
</script>
